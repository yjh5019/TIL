# github 명령어 정리

git remote add origin https://github.com/yjh5019/TIL.git

origin-->별명(복잡한 주소를 표시하기 위한 별명)

# 주의사항!!!!
1. 현재 위치를 잘 확인한다.--->중요!!
2. Repo 안에서 repo(master라는 글을 보고 repo라는것을 알 수 있음)을 또!!다시 만들지 않는다
 - 이미 git init 한 곳안에
 - 하위폴더를 만들고
 - 그 하위폴더에 다시 git init을 하지 않는다!!!
 - ㅡMaster 떠있으면 제발 init xxxxxxxx
3. Home(~)에서 init하지 않는다
4. 지금은 git hub 에서 직접 수정 하지 않는다!!!

# 추천 사항!!!!
1. 커밋 메세지는 짧지만 내용은 담아서
2. 모든 REPO는
   1. README.md
   2. .gitignore 담고 있도록 한다.
   3. 